Local branch for dev
TODO:
- Add Spring Security
- Add Transactions (DB Layer recommenden)
- Add roles(admin, user), Rules: Admin can post, add comments, delete post, delete comments. User reading only and comment posting

Note:
- Change DB_PORT in application.properties to ${DB_PORT:5433}, to build with docker
